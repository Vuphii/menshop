import styled from "styled-components";

export const WrapperLabelText = styled.h4`
  font-size: 18px;  // Kích thước font
  color: #333;      // Màu chữ
  margin: 0;        // Xóa margin mặc định
  padding: 0;       // Xóa padding mặc định
`;
export const WrapperTextValue = styled.span`
 font-size: 16px;   // Kích thước font
  color: #555;       // Màu chữ
  margin: 0;         // Xóa margin mặc định (nếu cần)
  padding: 0;        // Xóa padding mặc định (nếu cần)

`;
export const WrapperContent = styled.div`
 font-size: 14px;   // Kích thước font
  color: #333;       // Màu chữ
  margin: 0;         // Xóa margin mặc định (nếu cần)
  padding: 0;        // Xóa padding mặc định (nếu cần)
  display: flex;     // Hiển thị dư��i dạng flexbox
  flex-direction: column;
  gap: 12px

`;
export const WrapperPriceText = styled.span`
  font-size: 14px;   // Kích thước font
  color: #333;       // Màu chữ
  margin: 0;         // Xóa margin mặc định (nếu cần)
  padding: 0;        // Xóa padding mặc định (nếu cần)
`;